TF 1.x   python 3.7     TF1.15.0
TF 2.0+  python 3.8+



Tensor  在数学中是“张量”  TPU

标量，矢量/向量，张量
1、标量表示具体值
2、矢量表示位置（空间中的一个点）
3、张量表示整个空间

一维数组是矢量，多维数组是张量，矩阵也是张量；

4个重要的类型
1、Variable  计算图谱中的变量
2、Tensor    一个多维的矩阵，带有很多方法
3、Graph   一个计算图谱
4、Session  用来运行一个计算图谱（*************）

三个重要的数据函数：
1、Variable
   tf.Variable.__init__(
   initial_value = NONE,
   name = None
   dtype = None
   shape = None
   ......
   )

2、Constant 常数

3、Placeholder 暂时的变量（************）